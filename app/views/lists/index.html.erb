<p id="notice"><%= notice %></p>

<h1>Listas Públicas</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>owner</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lists.each do |list| %>
      <tr>
        <td><%= list.name %></td>
        <td><%= list.user.name %></td>
        <td><%= link_to 'Show', list %></td>
     </tr>
    <% end %>
  </tbody>
</table>
